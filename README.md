Run `npm install` to catch up with the packages installed.

Put new PDFs in public/handouts.
Then add the name of the pdf in the list in app/data/handouts.ts along with a title and description.
Then add it as an element in the main page.tsx.